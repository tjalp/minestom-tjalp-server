dependencies {
    implementation project(':peel')

    implementation "com.github.Minestom:Minestom:${project.lib_minestom_version}"
}

jar {
    manifest {
        attributes 'Main-Class': 'net.tjalp.peach.apple.Bootstrap',
                'Multi-Release': true
    }
}