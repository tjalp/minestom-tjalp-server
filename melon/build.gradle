dependencies {
    api project(':peel')

    compileOnly "com.velocitypowered:velocity-api:${project.lib_velocity_version}"

    kapt "com.velocitypowered:velocity-api:${project.lib_velocity_version}"
}